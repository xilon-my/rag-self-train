# Copy to .env (gitignored) and fill in.
KIMI_API_KEY=sk-...
# Mirror for China networks; huggingface.co is unreachable from the GPU node.
HF_ENDPOINT=https://hf-mirror.com
# Required only to publish model weights to Hugging Face (D4). Optional otherwise.
HF_TOKEN=hf_...
